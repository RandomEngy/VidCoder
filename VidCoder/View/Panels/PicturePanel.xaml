<UserControl
	x:Class="VidCoder.View.PicturePanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:VidCoder"
	xmlns:controls="clr-namespace:VidCoder.Controls"
	xmlns:model="clr-namespace:HandBrake.Interop;assembly=HandBrakeInterop"
	xmlns:resx="clr-namespace:VidCoder.Properties"
	xmlns:Encoding="clr-namespace:HandBrake.Interop.Model.Encoding;assembly=HandBrakeInterop" mc:Ignorable="d" 
    d:DesignHeight="330" d:DesignWidth="622">
	<UserControl.Resources>
		<local:EnumDisplayer Type="{x:Type Encoding:Anamorphic}" x:Key="AnamorphicChoices" />
	</UserControl.Resources>
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="305" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Grid Grid.Row="0" Grid.Column="0" Visibility="{Binding HasSourceData, Converter={StaticResource VisibilityConverter}}">
			<Border
				Margin="3,3,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="274"
				Style="{StaticResource InputOutputInfoSection}"
				Background="{StaticResource InputBrush}" Effect="{StaticResource InfoBoxShadow}">
				<Grid>
					<TextBlock Height="21" Width="38" HorizontalAlignment="Left" Margin="2,0,0,0" VerticalAlignment="Top" Text="Input" Style="{StaticResource SectionHeader}" />
					<Grid Margin="50,0,0,0">
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
						</Grid.RowDefinitions>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="105" />
							<ColumnDefinition Width="4" />
							<ColumnDefinition Width="173*" />
						</Grid.ColumnDefinitions>
						<TextBlock Text="Storage Resolution:" Grid.Row="0" Grid.Column="0" />
						<TextBlock Text="Pixel Aspect Ratio:" Grid.Row="1" Grid.Column="0" />
						<TextBlock Text="Display Resolution:" Grid.Row="2" Grid.Column="0" />

						<TextBlock Text="{Binding InputSourceResolution}" Grid.Row="0" Grid.Column="2" />
						<TextBlock Text="{Binding InputPixelAspectRatio}" Grid.Row="1" Grid.Column="2" />
						<TextBlock Text="{Binding InputDisplayResolution}" Grid.Row="2" Grid.Column="2" />
					</Grid>
				</Grid>
			</Border>
		</Grid>

		<!-- Sizing block -->
		<Grid Grid.Row="1">
			<TextBlock
				Height="21" HorizontalAlignment="Left" Margin="6,2,0,0" VerticalAlignment="Top" Width="64"
				Text="Sizing"
				Style="{StaticResource SectionHeader}" />
			<Label HorizontalAlignment="Left" Margin="45,22,0,0" Name="label1" Width="47" Height="27.04" VerticalAlignment="Top">Width:</Label>
			<controls:NumberBox
				Height="23" HorizontalAlignment="Left" Margin="90,25,0,0" VerticalAlignment="Top" Width="60"
				Number="{Binding Width, Mode=TwoWay}" NoneCaption="(auto)" Modulus="2" Minimum="46" Maximum="3712"
				IsEnabled="{Binding WidthEnabled}"/>
			<Label HorizontalAlignment="Left" Margin="181,22,0,0" Name="label2" Width="49.2" Height="27.04" VerticalAlignment="Top">Height:</Label>
			<controls:NumberBox
				Height="23" Margin="231,25,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="60"
				Number="{Binding Height, Mode=TwoWay}" NoneCaption="(auto)" Modulus="2" Minimum="46" Maximum="3712"
				IsEnabled="{Binding HeightEnabled}"/>
			<Label HorizontalAlignment="Left" Margin="20,49,0,0" Name="label3" Width="77" Height="26" VerticalAlignment="Top">Max Width:</Label>
			<controls:NumberBox
				HorizontalAlignment="Left" Margin="90,54,0,0" Width="60" Height="23" VerticalAlignment="Top"
				Number="{Binding MaxWidth, Mode=TwoWay}" Modulus="2" Minimum="46"
				IsEnabled="{Binding WidthEnabled}"/>
			<Label Margin="156,49,0,0" Name="label4" Height="28.04" VerticalAlignment="Top" HorizontalAlignment="Left" Width="74.2">Max Height:</Label>
			<controls:NumberBox
				Margin="231,54,0,0" Height="23.04" VerticalAlignment="Top" HorizontalAlignment="Left" Width="60"
				Number="{Binding MaxHeight, Mode=TwoWay}" Modulus="2" Minimum="46"
				IsEnabled="{Binding HeightEnabled}"/>

			<CheckBox
				HorizontalAlignment="Left" Margin="48,81,0,0" Name="checkBox1" Width="120" Height="17" VerticalAlignment="Top"
				IsChecked="{Binding KeepDisplayAspect}"
				IsEnabled="{Binding KeepDisplayAspectEnabled}">Keep Aspect Ratio</CheckBox>


			<Grid HorizontalAlignment="Left" Margin="20,103,0,0" Name="grid2" Width="234" VerticalAlignment="Top">
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="110" />
					<ColumnDefinition Width="139*" />
				</Grid.ColumnDefinitions>

				<Label Grid.Row="0" Grid.Column="0" Height="24" HorizontalAlignment="Left" VerticalAlignment="Center" Width="81">Anamorphic:</Label>
				<ComboBox
					Grid.Column="1" Height="22" VerticalAlignment="Top" Margin="0,1,0,0" HorizontalAlignment="Left" Width="65"
					ItemsSource="{Binding DisplayNames, Source={StaticResource AnamorphicChoices}}"
					SelectedValue="{Binding Anamorphic, Converter={StaticResource AnamorphicChoices}}" />

				<Label
					Grid.Row="1" Grid.Column="0" Height="24" HorizontalAlignment="Left" VerticalAlignment="Center" Width="81"
					Visibility="{Binding CustomAnamorphicFieldsVisible, Converter={StaticResource VisibilityConverter}}">Modulus:</Label>
				<ComboBox
					Grid.Row="1" Grid.Column="1" Height="22" VerticalAlignment="Top" Margin="0,1,0,0" HorizontalAlignment="Left" Width="65"
					SelectedValue="{Binding Modulus}"
					SelectedValuePath="Content"
					Visibility="{Binding CustomAnamorphicFieldsVisible, Converter={StaticResource VisibilityConverter}}">
					<ComboBoxItem>16</ComboBoxItem>
					<ComboBoxItem>8</ComboBoxItem>
					<ComboBoxItem>4</ComboBoxItem>
					<ComboBoxItem>2</ComboBoxItem>
				</ComboBox>
			</Grid>

			<RadioButton
				Margin="37,160,0,0" Height="16" HorizontalAlignment="Left" VerticalAlignment="Top"
				Visibility="{Binding CustomAnamorphicFieldsVisible, Converter={StaticResource VisibilityConverter}}"
				IsChecked="{Binding UseDisplayWidth}"
				ToolTip="{x:Static resx:Resources.DisplayWidthToolTip}">Display Width:</RadioButton>
			<controls:NumberBox
				Height="23" HorizontalAlignment="Left" Margin="155,156,0,0" VerticalAlignment="Top" Width="60" NoneCaption="(auto)"
				Number="{Binding DisplayWidth, Mode=TwoWay}" AllowEmpty="{Binding KeepDisplayAspect}" Minimum="46"
				Visibility="{Binding CustomAnamorphicFieldsVisible, Converter={StaticResource VisibilityConverter}}"
				IsEnabled="{Binding UseDisplayWidth}"
				ToolTip="{x:Static resx:Resources.DisplayWidthToolTip}"/>

			<RadioButton
				Margin="37,185,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="119"
				Visibility="{Binding PixelAspectVisibile, Converter={StaticResource VisibilityConverter}}"
				IsChecked="{Binding UseDisplayWidth, Converter={StaticResource InverseBoolConverter}}"
				ToolTip="{x:Static resx:Resources.PixelAspectRatioToolTip}">Pixel Aspect Ratio:</RadioButton>
			<controls:NumberBox
				Height="23" HorizontalAlignment="Left" Margin="155,183,0,0" VerticalAlignment="Top" Width="45"
				Number="{Binding PixelAspectX, Mode=TwoWay}" AllowEmpty="False" Minimum="1"
				Visibility="{Binding PixelAspectVisibile, Converter={StaticResource VisibilityConverter}}"
				IsEnabled="{Binding UseDisplayWidth, Converter={StaticResource InverseBoolConverter}}"
				ToolTip="{x:Static resx:Resources.PixelAspectRatioToolTip}"/>
			<Label
				Height="23.562" HorizontalAlignment="Left" VerticalContentAlignment="Center" Margin="203,181.356,0,0" Name="label5" VerticalAlignment="Top" Width="16.422"
				Visibility="{Binding PixelAspectVisibile, Converter={StaticResource VisibilityConverter}}">/</Label>
			<controls:NumberBox
				Height="23" HorizontalAlignment="Left" Margin="220,183,0,0" VerticalAlignment="Top" Width="45"
				Number="{Binding PixelAspectY, Mode=TwoWay}" AllowEmpty="False" Minimum="1"
				Visibility="{Binding PixelAspectVisibile, Converter={StaticResource VisibilityConverter}}"
				IsEnabled="{Binding UseDisplayWidth, Converter={StaticResource InverseBoolConverter}}"
				ToolTip="{x:Static resx:Resources.PixelAspectRatioToolTip}"/>
		</Grid>

		<Grid Grid.Row="2" Grid.Column="0" Visibility="{Binding HasSourceData, Converter={StaticResource VisibilityConverter}}">
			<Border
				HorizontalAlignment="Left" VerticalAlignment="Top" Margin="3,5,0,0"
				Style="{StaticResource InputOutputInfoSection}" Height="55.88" Width="270"
				Background="{StaticResource OutputBrush}" Effect="{StaticResource InfoBoxShadow}">
				<Grid>
					<TextBlock Text="Output" Height="21" HorizontalAlignment="Left" Margin="2,0,0,0" VerticalAlignment="Top" Width="54" Style="{StaticResource SectionHeader}" />
					<Grid Margin="50,0,0,0">
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
						</Grid.RowDefinitions>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="105" />
							<ColumnDefinition Width="4" />
							<ColumnDefinition Width="173*" />
						</Grid.ColumnDefinitions>
						<TextBlock Text="Storage Resolution:" Grid.Row="0" Grid.Column="0" />
						<TextBlock Text="Pixel Aspect Ratio:" Grid.Row="1" Grid.Column="0" />
						<TextBlock Text="Display Resolution:" Grid.Row="2" Grid.Column="0" />

						<TextBlock Text="{Binding OutputSourceResolution}" Grid.Row="0" Grid.Column="2" />
						<TextBlock Text="{Binding OutputPixelAspectRatio}" Grid.Row="1" Grid.Column="2" />
						<TextBlock Text="{Binding OutputDisplayResolution}" Grid.Row="2" Grid.Column="2" />
					</Grid>
				</Grid>
			</Border>
		</Grid>
		<TextBlock Grid.Column="1" Grid.RowSpan="3" Height="21" Margin="50,6.96,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="83"
						Text="Cropping"
						Style="{StaticResource SectionHeader}" />

		<RadioButton
			Height="16" Grid.Column="1" Grid.RowSpan="3"  HorizontalAlignment="Left" Margin="50,34,0,0" Name="radioButton1" VerticalAlignment="Top" Width="120"
			IsChecked="{Binding CustomCropping, Converter={StaticResource InverseBoolConverter}}">Automatic</RadioButton>
		<RadioButton
			Height="16" Grid.Column="1" Grid.RowSpan="3"  HorizontalAlignment="Left" Margin="50,56,0,0" Name="radioButton2" VerticalAlignment="Top" Width="120"
			IsChecked="{Binding CustomCropping}">Custom</RadioButton>
		<Grid Margin="45.434,70,0,0" Name="grid4" Grid.Column="1" Height="139.04" VerticalAlignment="Top" Grid.RowSpan="3" HorizontalAlignment="Left" Width="210">
			<controls:NumberBox
				Height="23" Margin="74,28,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="45"
				Number="{Binding CropTop, Mode=TwoWay}" AllowEmpty="False" Minimum="0" Modulus="2"
				IsEnabled="{Binding CustomCropping}"/>
			<controls:NumberBox
				Margin="111,56,0,0" Height="22.96" VerticalAlignment="Top" HorizontalAlignment="Left" Width="45"
				Number="{Binding CropRight, Mode=TwoWay}" AllowEmpty="False" Minimum="0" Modulus="2"
				IsEnabled="{Binding CustomCropping}"/>
			<controls:NumberBox
				Margin="37,56,0,0" HorizontalAlignment="Left" Width="45" Height="23" VerticalAlignment="Top"
				Number="{Binding CropLeft, Mode=TwoWay}" AllowEmpty="False" Minimum="0" Modulus="2"
				IsEnabled="{Binding CustomCropping}"/>
			<controls:NumberBox
				Height="23" Margin="74,82,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="45"
				Number="{Binding CropBottom, Mode=TwoWay}" AllowEmpty="False" Minimum="0" Modulus="2"
				IsEnabled="{Binding CustomCropping}"/>
			<Label Height="28" Margin="81,4,0,0" Name="label7" VerticalAlignment="Top" HorizontalAlignment="Left" Width="38.019">Top</Label>
			<Label Margin="7,54,0,0" Name="label8" HorizontalAlignment="Left" Width="33" Height="28" VerticalAlignment="Top">Left</Label>
			<Label Height="28" Margin="72,101,0,0" Name="label9" VerticalAlignment="Top" HorizontalAlignment="Left" Width="53.566">Bottom</Label>
			<Label HorizontalAlignment="Left" Margin="155.64,54,0,0" Name="label10" Width="53.36" Height="28.225" VerticalAlignment="Top">Right</Label>
		</Grid>
	</Grid>
</UserControl>
