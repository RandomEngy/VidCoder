namespace VidCoderCommon.Model
{
    public enum VCDenoise
    {
        Off = 0,
        hqdn3d = 1,
        NLMeans = 2,

		// Obsolete. This is now its own filter dropdown.
		ChromaSmooth = 3
    }
}
